<Page
  x:Class="test.Views.DownloadsPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:models="using:test.Models"
  mc:Ignorable="d"
>
  <Page.Resources>
    <Style x:Key="TransparentButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="8" />
    </Style>
    <Style
      x:Key="IconButtonStyle"
      BasedOn="{StaticResource TransparentButtonStyle}"
      TargetType="Button"
    >
      <Setter Property="MinWidth" Value="32" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="HorizontalAlignment" Value="Center" />
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="HorizontalContentAlignment" Value="Center" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
  </Page.Resources>
  <ScrollViewer>
    <StackPanel Margin="{StaticResource NavigationViewPageContentMargin}" Padding="16" Spacing="12">
      <!--  Header (scrolls with content)  -->
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Style="{ThemeResource TitleTextBlockStyle}" Text="Downloads" />
        <Button
          Grid.Column="1"
          Click="OpenFolderButton_Click"
          Content="Open folder"
          Style="{ThemeResource AccentButtonStyle}"
        />
      </Grid>
      <ListView
        x:Name="DownloadsList"
        IsItemClickEnabled="True"
        ItemClick="DownloadsList_ItemClick"
        ItemsSource="{x:Bind ViewModel.Downloads, Mode=OneWay}"
        SelectionMode="None"
      >
        <ListView.ItemContainerStyle>
          <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,4" />
          </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="models:DownloadItem">
            <Grid
              Padding="16"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
              BorderThickness="1"
              ColumnSpacing="12"
              CornerRadius="8"
              RowSpacing="10"
            >
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
              </Grid.RowDefinitions>
              <!--  Icon  -->
              <Border
                Grid.RowSpan="2"
                Grid.Column="0"
                Width="48"
                Height="48"
                VerticalAlignment="Center"
                CornerRadius="8"
              >
                <Image Source="{x:Bind LogoUrl, Mode=OneWay}" Stretch="UniformToFill" />
              </Border>
              <!--  Title + (non-downloading) status  -->
              <Grid Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" RowSpacing="6">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
                  Grid.Row="0"
                  FontWeight="SemiBold"
                  Text="{x:Bind Title, Mode=OneWay}"
                  TextTrimming="CharacterEllipsis"
                />
                <TextBlock
                  Grid.Row="1"
                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                  Style="{ThemeResource CaptionTextBlockStyle}"
                  Text="{Binding StatusText, Mode=OneWay}"
                  Visibility="{Binding Status, Converter={StaticResource NotDownloadingStatusToVisibilityConverter}, Mode=OneWay}"
                />
                <!--  download progressbar section  -->
                <Grid
                  Grid.Row="1"
                  Margin="0,5,0,0"
                  VerticalAlignment="Center"
                  Visibility="{Binding Status, Converter={StaticResource DownloadStatusToVisibilityConverter}, Mode=OneWay}"
                >
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <ProgressBar
                    Grid.Column="0"
                    VerticalAlignment="Center"
                    Maximum="100"
                    Value="{Binding Progress, Mode=OneWay}"
                  />
                  <TextBlock
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="{Binding Progress, Mode=OneWay, Converter={StaticResource ProgressToPercentTextConverter}}"
                  />
                </Grid>
              </Grid>
              <!--  Cancel (downloading) and Options (non-downloading) share the same cell for perfect alignment  -->
              <Button
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="2"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Click="CancelButton_Click"
                Style="{StaticResource IconButtonStyle}"
                Tag="{x:Bind ProductId}"
                ToolTipService.ToolTip="Cancel download"
                Visibility="{Binding Status, Converter={StaticResource DownloadStatusToVisibilityConverter}, Mode=OneWay}"
              >
                <FontIcon FontSize="12" Glyph="" />
              </Button>
              <Button
                Grid.Row="0"
                Grid.RowSpan="2"
                Grid.Column="2"
                VerticalAlignment="Center"
                VerticalContentAlignment="Center"
                Style="{StaticResource IconButtonStyle}"
                Visibility="{Binding Status, Converter={StaticResource NotDownloadingStatusToVisibilityConverter}, Mode=OneWay}"
              >
                <FontIcon FontSize="16" Glyph="" />
                <Button.Flyout>
                  <MenuFlyout>
                    <MenuFlyoutItem
                      Click="DeleteMenuItem_Click"
                      Tag="{x:Bind ProductId}"
                      Text="Delete"
                    >
                      <MenuFlyoutItem.Icon>
                        <FontIcon Glyph="" />
                      </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                  </MenuFlyout>
                </Button.Flyout>
              </Button>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackPanel>
  </ScrollViewer>
</Page>
